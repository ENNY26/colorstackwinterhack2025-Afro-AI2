# Server Configuration
NODE_ENV=development
PORT=5000

# MongoDB Configuration
MONGODB_URI=mongodb://localhost:27017/afro_ai
# For MongoDB Atlas: mongodb+srv://<username>:<password>@cluster.mongodb.net/afro_ai

# JWT Configuration
JWT_SECRET=your-super-secret-jwt-key-change-in-production
JWT_EXPIRES_IN=7d

# Twilio Verify (SMS codes for signup & login)
# https://console.twilio.com — create a Verify Service
TWILIO_ACCOUNT_SID=your-twilio-account-sid
TWILIO_AUTH_TOKEN=your-twilio-auth-token
TWILIO_VERIFY_SERVICE_SID=your-verify-service-sid
# Without Twilio configured, dev mode accepts OTP code: 123456

# Groq API (RECOMMENDED for Speech-to-Text — free & fast Whisper)
# Get a free key at https://console.groq.com/keys
# When set, Groq is used first for transcription (falls back to local/OpenAI).
GROQ_API_KEY=gsk_your-groq-api-key
# GROQ_WHISPER_MODEL=whisper-large-v3-turbo

# OpenAI API (for Whisper Speech-to-Text fallback, optional for GPT)
# Note: If you only use Groq for transcription, this is optional.
OPENAI_API_KEY=sk-your-openai-api-key

# Transcription provider order: groq_first | local_first | openai_first | groq | local | openai
# Default: groq_first when GROQ_API_KEY is set, else local_first
# TRANSCRIBE_PROVIDER=groq_first

# Anthropic Claude API (for AI Tutor - RECOMMENDED if you have credits)
ANTHROPIC_API_KEY=sk-ant-your-anthropic-api-key

# ElevenLabs API (for Text-to-Speech)
# Free tier: cannot use "library" voices via API; app will fall back to device TTS.
# Leave unset or use a voice from your account that works on free tier.
ELEVENLABS_API_KEY=your-elevenlabs-api-key
ELEVENLABS_VOICE_ID=your-preferred-voice-id
# Optional: cloned Yoruba voice (falls back to ELEVENLABS_VOICE_ID)
# ELEVENLABS_YORUBA_VOICE_ID=your-yoruba-cloned-voice-id
# ELEVENLABS_MODEL_ID=eleven_multilingual_v2

# ElevenLabs voice_settings (tune without code changes)
ELEVENLABS_STABILITY=0.45
ELEVENLABS_SIMILARITY=0.75
ELEVENLABS_STYLE=0.0
ELEVENLABS_SPEED=1.08
ELEVENLABS_SPEAKER_BOOST=true
# Set true to apply per-language presets in elevenLabsService.js (default: env only)
# ELEVENLABS_USE_LANGUAGE_PRESETS=false

# --- Azure Neural TTS (cheap voice for well-supported languages) ---
# ~10x cheaper than ElevenLabs, 500K chars/month free. Create a "Speech"
# resource in the Azure portal, then copy its Key + Region here.
# Get: portal.azure.com -> Create resource -> "Speech" -> Keys and Endpoint
# AZURE_SPEECH_KEY=your-azure-speech-key
# AZURE_SPEECH_REGION=eastus
# Optional per-language voice overrides (defaults shown):
# AZURE_SWAHILI_VOICE=sw-KE-ZuriNeural
# AZURE_ZULU_VOICE=zu-ZA-ThandoNeural
# AZURE_AMHARIC_VOICE=am-ET-MekdesNeural
# AZURE_SOMALI_VOICE=so-SO-UbaxNeural

# --- TTS routing (which provider speaks each language) ---
# Defaults: Yoruba/Igbo/Akan/Hausa -> elevenlabs; Swahili/Zulu/Amharic/Xhosa/Somali -> azure.
# If a provider isn't configured or lacks the language, it falls back to the other,
# then to free on-device TTS. Override per language, e.g.:
# TTS_ROUTE_SWAHILI=elevenlabs
# TTS_DEFAULT_PROVIDER=elevenlabs
# Set to false to disable audio caching (repeated phrases re-synthesize):
# TTS_CACHE=true

# AI Model Selection (openai or anthropic)
# Set to 'anthropic' to use Claude for conversations
AI_PROVIDER=anthropic

# Rate Limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# FastAPI ai_speech_service (Whisper + pronunciation). Required for POST /api/audio/practice-round
AI_SPEECH_SERVICE_URL=http://127.0.0.1:8000

# Audio File Settings
MAX_AUDIO_FILE_SIZE=10485760
AUDIO_UPLOAD_DIR=./uploads/audio

# Cors Settings
CORS_ORIGIN=http://localhost:8081

# INSTRUCTIONS:
# 1. Copy this file and rename to .env
# 2. Fill in your actual API keys
# 3. Never commit the .env file to version control

